# n8n Webhook URLs
# ==================
# These webhooks receive lead data and demo action triggers
# Configure these in your n8n instance and add them here

# Lead creation webhook - triggered when a new lead is captured
N8N_LEAD_WEBHOOK_URL=https://your-n8n-instance.com/webhook/lead

# Demo actions webhook - triggered when demo buttons are clicked
N8N_DEMO_WEBHOOK_URL=https://your-n8n-instance.com/webhook/demo

# Chat webhook - triggered when users send messages on /workwithben page
N8N_CHAT_WEBHOOK_URL=https://your-n8n-instance.com/webhook/chat

# Stripe Configuration
# ==================
# Get these from your Stripe Dashboard (https://dashboard.stripe.com/test/apikeys)

# Stripe Secret Key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=sk_test_your_secret_key_here

# Stripe Webhook Secret (starts with whsec_)
# Create this at https://dashboard.stripe.com/webhooks
# Make sure to add the endpoint: https://your-domain.com/api/stripe/webhook
# And select the event: checkout.session.completed
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Airtable Configuration (for Stripe webhook integration)
# ==================
AIRTABLE_BASE_ID=your_airtable_base_id
AIRTABLE_TOKEN=your_airtable_personal_access_token

